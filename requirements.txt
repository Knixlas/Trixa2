# Trixa2 — kärnberoenden för engine + trixa-skikt + (kommande) FastAPI/Railway.
# Garmin-MCP har egna deps i garmin-mcp/requirements.txt och är fristående.

# DB-klient — postgrest direkt (supabase metapackage drar in pyiceberg som
# inte bygger mot Python 3.14). postgrest räcker för Trixa: vi behöver
# CRUD mot tabeller, inte realtime/auth/storage.
postgrest>=2.0.0

# YAML för engine-konfig och passbank
pyyaml>=6.0

# .env-stöd för lokal utveckling
python-dotenv>=1.0.0

# Trixa-API (steg 6)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
python-multipart>=0.0.6

# Templates för formulär-UI (steg 7)
jinja2>=3.1

# HTTP-klient för Supabase Auth (GoTrue REST) i trixa_api/supabase_auth.py
requests>=2.31.0
